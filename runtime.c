#include <stdio.h>
#include <stdlib.h>

// هذه الدالة سيستدعيها مترجمنا عند اكتشاف محاولة قسمة على صفر
void panic_div_zero() {
    printf("\n[خطأ وقت التشغيل]: تم إيقاف البرنامج بأمان! محاولة قسمة على صفر.\n");
    exit(1); // إنهاء البرنامج فورًا برمز خطأ
}